* Test on Windows
* Some shim functions put smali files in each directory where a used permission was found.  Instead, place one smali file in a global location and refer to it.
* Replace use of external 'diff' command with built-in difflib module.
* Make AppTroller.py into a class.

Contacts:
  * Create subclass of CursorLoader.
  * Handle insert/delete/update for contacts

